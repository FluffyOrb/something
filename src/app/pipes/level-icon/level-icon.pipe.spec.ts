import { LevelIconPipe } from './level-icon.pipe';

describe('LevelIconPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelIconPipe();
    expect(pipe).toBeTruthy();
  });
});
